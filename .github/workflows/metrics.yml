name: Metrics

on:
  schedule:
    - cron: "0 0 * * *"   # daily
  workflow_dispatch:

permissions:
  contents: write

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Generate metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: noobcoder1982
          template: classic
          filename: github-metrics.svg

          # Big dashboard sections
          base: header, activity, community, repositories, metadata
          config_display: large
          config_animations: yes

          # “Everything” (safe, no extra external accounts)
          plugin_languages: yes
          plugin_languages_details: bytes-size, percentage
          plugin_languages_sections: most-used
          plugin_languages_limit: 12

          plugin_habits: yes
          plugin_habits_facts: yes
          plugin_habits_charts: yes

          plugin_activity: yes
          plugin_activity_limit: 15
          plugin_activity_days: 30

          plugin_achievements: yes
          plugin_achievements_threshold: C
          plugin_achievements_display: detailed
          plugin_achievements_secrets: yes

          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year

          # PageSpeed (your portfolio)
          plugin_pagespeed: yes
          plugin_pagespeed_url: https://brewedframe.me
          plugin_pagespeed_detailed: true
          plugin_pagespeed_screenshot: true
